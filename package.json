{
  "name": "linkedin-job-eval",
  "version": "0.1.0",
  "description": "AI-assisted LinkedIn job evaluation (MVP) — is this job worth applying?",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "mammoth": "^1.8.0",
    "pdfjs-dist": "^4.7.76"
  }
}
